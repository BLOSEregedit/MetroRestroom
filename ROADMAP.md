# MetroRestroom 开发路线图

更新时间：2026-08-21

## 当前阶段

本地真实数据、线路拓扑、定位、云开发真实写入链路和首页第三轮顶部控制改版已完成，进入真机验收与上线准备阶段。

第一版目标是覆盖上海地铁 1-18 号线及浦江线，达到可真实使用、可提交微信小程序审核的标准。

当前开发分支与远端目标分支均为 `main`；本轮线路拓扑交互调整状态见「最近验证」。

## 已完成

- 完成第一版 PRD、首页交互、个人中心和技术方案确认。
- 完成微信原生小程序双 Tab 骨架，未使用跨平台框架。
- 完成首页 Skyline／Worklet 原生线路拨轮、方向切换、手动锚点、多厕所抽屉、最近记录及逐站声音／触感反馈；其他页面继续使用 WebView，未接入第三方框架。
- 完成首页第二轮交互改版：恢复苹果风和原字号，一屏固定 5 站，左侧连续轨道与定位针／可点击圆点、右卡独立高亮、显式换乘线路胶囊，以及按线路分组的多厕所抽屉。
- 完成首页第三轮顶部控制改版：城市胶囊与第一版城市面板、两行控制卡、站名选起点、“自选起点｜恢复定位”，以及绿色／蓝色同步圆点、紧凑时间和常驻轻量更新入口。
- 修复“恢复定位”无反馈：已有上次定位站时立即恢复智能计算起点并保持当前浏览站不跳动，后台定位未匹配时继续使用上次位置并显示重试；用户曾拒绝权限时由点击动作进入微信授权设置。
- 完成智能定位范围与换乘选线重构：3 公里内自动采用最近物理站，超过 3 公里时列出 5 公里内全部物理站并按距离由近到远选择，5 公里内无站则转手动选择；换乘站仅在入口关系覆盖该站全部线路、GPS 精度足够且最近入口稳定唯一时自动选线，否则依次使用本会话显式线路、同站历史线路或一次线路选择，未确认时不覆盖原起点。
- 完成定位兜底的两步手动选择引导：复用既有线路与站点面板显示“先选择线路 → 再选择起点站”，不增加复合选择器；5、10、11 号线的手动站点列表合并全部有效支线并按线路站 ID 去重。
- 完成环线与个人页系统信息收敛：4 号线顶部只显示官方圈向“内圈／外圈”，线路下拉箭头几何居中；厕所数据同步状态移到个人页，关于页仅在正式版显示真实版本号，不增加手动小程序更新入口。
- 完成厕所卡片结构化展示：“卫生间”／“卫生间 · 多处”状态胶囊、闸区／站内外／出口／换乘通道或方向车厢标签、固定单行布局、抽屉完整原文，以及任意可见卡位整卡打开详情。
- 恢复微信原生 TabBar，由客户端稳定渲染首页／我的图标和文字；个人页继续移除顶部 logo／标题／副标题，并以最近记录作为首个模块。
- 完成 Excel 确定性导入：19 张工作表、519 条源记录全部保留。
- 完成 1—18 号线及浦江线真实数据接入；当前 518 个已开通站点可浏览。
- 完成 4 号线环线、5／10／11 号线支线、同名换乘、别名和错误换乘排除。
- 完成支线默认主线路径、单入口端点切换、分支站间中点快捷位、4 号线双箭头闭环导视与首尾跳转、普通线路同心圆端点、旧站点 ID 兼容和未开通站点兜底。
- 通过产品、交互、技术三路 Agent 验收和微信开发者工具实测。

## 待办任务

### 阶段三：定位

- [x] 补齐 411 个物理站坐标，保留 OSM 固定快照、许可、确定性构建和 10 项人工绑定。
- [x] 接入定位授权、拒绝授权和定位失败的降级流程；坐标未就绪时不会请求权限。
- [x] 完成 1.2／3／5 公里距离分层：3 公里内自动采用最近物理站，超过 3 公里时展示 5 公里内全部物理站并按距离排序，5 公里内无站时提示手动选择。
- [x] 接入 OSM 入口固定快照、确定性构建、ODbL 来源说明及换乘线路保守判定；入口关系不完整、共享、未知或 GPS 精度不足时不自动猜线。
- [x] 完成手动方向锁定和恢复智能定位；持续定位与自动方向判断留到真机验证后评估。
- [ ] 真机验证定位精度和地铁弱信号场景。

### 阶段四：云开发

- [x] 配置云开发环境 ID `metro-restroom-d4goyb1fq3f9df0b3`。
- [x] 本地接入匿名云开发能力，不增加显式登录；服务端只保存 `OPENID` 的不可逆哈希。
- [x] 本地接入数据版本检查，本地基础数据在断网或云端失败时仍可使用。
- [x] 完成厕所数据纠错表单、上下文自动带入、本地失败草稿、云函数幂等和事务限流。
- [x] 关联小程序 AppID，创建三个仅管理端可读写的集合并部署 `metroRestroomApi`。
- [x] 在目标环境完成版本检查和小程序真实纠错写入联调。
- [ ] 在目标环境继续完成真实幂等和限流联调；本地自动化验证已通过。
- [x] 保持最近记录和用户偏好仅存本地。
- [x] 本地将全局版本提示升级为按城市／线路的固定 12 小时缓存同步；跨线只批量检查路径涉及的缺失或过期线路。
- [x] 增加最近同步状态与手动刷新：首页与个人页当前年份使用 `MM-DD HH:mm`、跨年份使用完整年份；绿色表示 12 小时内已核对，蓝色表示本地可用；成功冷却 5 分钟，失败保留上次同步时间并允许重试。
- [x] 扩展本地路径结果，返回途经站点、换乘点和线路；路径及 ETA 查询不依赖云函数。
- [x] 部署新版 `metroRestroomApi`，并向 `data_versions` 写入及回读验证上海 manifest 与首批 19 条线路状态快照。
- [ ] 后续逐步用相邻站、换乘和厕所步行边权提高 ETA 精度。

### 阶段五：上线准备

- [x] 完成首页交互密度、方向切换、换乘切线和多厕所线路归属的本地回归。
- [x] 完成首页第三轮顶部控制、城市面板、自选起点恢复和同步状态的自动化及微信开发者工具模拟器验收。
- [ ] 在 iOS／Android 真机验收首页五站密度、横纵手势、连续轨道、定位针／圆点锚点和换乘抽屉。
- [ ] 补齐微信小程序隐私保护指引。
- [x] 清理未启用的 CloudBase 快速开始示例页面、组件和无引用资源；业务 Tab 图标及提示音保留。
- [x] 完成 19 条线路、环线、支线、换乘和多厕所回归测试。
- [ ] 完成不同机型、弱网和拒绝权限场景真机验收。
- [ ] 包体代码质量检查已通过；继续检查隐私权限、审核文案和小程序截图。
- [ ] 用户确认后上传审核版本并提交微信审核。

## 当前阻塞

- 微信开发者工具 RC 2.02.2608031 偶发 `appServiceSDKScriptError timeout`；同期云函数均正常返回，真实幂等和限流仍需稳定版／真机复核。
- 上线审核前需要完成隐私保护指引。

## 最近验证

- 2026-08-21：仅调整分支站与下一站之间的线路切换位文案，顶部支线路径切换保持现状。5、10、11 号线根据目标路径显示“切换至主线／支线 · 终点方向”：例如主线上的龙溪路显示“切换至支线 · 航中路方向”，支线反向显示“切换至主线 · 虹桥火车站方向”。站间提示与顶部入口拆分文案派生但继续复用同一切换行为；六种正反路径及顶部文案不变回归、数据、运行时、定位、首页交互、JavaScript 语法与差异检查全部通过。
- 2026-08-21：完善 5／10／11 号线的手动选站支线语义，不新增“选择支线”步骤。第二步列表按“共线段／主线／支线”分组，共线站删除“两条支线均可”；主线与支线独有站将方向缩写为“站名 · 终点方向”并在同一行显示，主线／支线采用官方线路归属。全部站点继续按线路站 ID 去重；选择独有站时自动提交所属路径，并在原方向不适用时切到该路径默认方向，选择共线站或在手动流程中重新选择当前线路时保留当前合法路径与方向。5 号线 19 站、10 号线 37 站、11 号线 38 站的分组、数量、唯一性和单行文案回归通过；数据、运行时、定位、入口、首页交互、同步、个人中心和云开发 8 组验收、相关 JavaScript 语法、WXML／WXSS 编译及差异检查全部通过。
- 2026-08-21：修复定位兜底两步手动选择无法回退的问题。“再选择起点站”面板右上角新增蓝色“上一步”，返回后保持手动选择流程和已选浏览线路，清空旧线路站点列表，但不修改原计算起点。复核 4 号线尾部的“上海体育场”和“上海体育馆”为两个真实相邻车站，不做错误合并；回归锁定 4 号线共 26 个唯一线路站 ID，两站各出现一次。数据、运行时、定位、入口、首页交互、同步、个人中心和云开发 8 组回归、JavaScript 语法及差异检查全部通过；微信开发者工具确认右上角入口未挤压标题，触发后线路面板重新打开、站点面板关闭、原起点不变，临时验证状态随后已清理。
- 2026-08-21：将附近站候选面板和同站线路确认面板底部的兜底入口由“手动选择”改为“选择其他”，读屏名称同步改为“选择其他线路和起点站”；5 公里内无站时的顶部动作仍保留“手动选择”，避免无候选参照时语义不完整。数据、运行时、定位、入口、首页交互、同步、个人中心和云开发 8 组回归及差异检查全部通过。微信开发者工具临时模拟坐标 `31.37482, 121.15`，最近的嘉定西站直线距离约 6.98 公里；页面正确显示“5公里内无站｜手动选择”，候选数为 0，原计算起点未被覆盖，随后已恢复真实定位接口。
- 2026-08-21：将定位兜底的“手动选择”改为两步复用流程。点击后先打开既有线路面板并显示“先选择线路”，选线同步切换浏览线路后自动打开既有站点面板并显示“再选择起点站”；不新增线路下拉或复合选择器。手动站点列表独立于当前支线路径，5、10、11 号线合并全部有效支线并按线路站 ID 去重，11 号线回归同时覆盖花桥和嘉定北。关闭第一步不改线路或起点，关闭第二步保留已明确选择的浏览线路但不改原计算起点，确认站点后结束引导并进入自选起点。数据、运行时、定位、入口、首页交互、同步、个人中心和云开发 8 组回归、JavaScript 语法、差异检查及首页 WXML／WXSS 编译全部通过。Nightly 自动化在跨线路大列表更新期间连续超时并自动重载，控制台无业务错误；第二步面板的最终触摸视觉仍列入模拟器手动点击／真机复核。
- 2026-08-21：完成智能定位范围与换乘线路解析重构，并按最终交互将 3—5 公里单站确认改为附近站列表。不超过 1.2 公里显示“智能定位”，1.2—3 公里自动使用最近物理站并显示直线距离；最近站超过 3 公里时列出定位点 5 公里内全部物理站，按直线距离由近到远排列并逐项标注距离，选择前不覆盖原计算起点；5 公里内无站时提示“当前定位 5 公里内无站点，请手动选择”。GPS 精度跨越 3 公里边界时按保守层级进入列表。新增 OSM 入口固定快照与确定性构建链路，1,485 个源入口中 1,463 个安全进入运行时，覆盖 364／411 个物理站和 451／518 个线路站；换乘站只有在全部有效线路均有入口关系覆盖、定位精度不超过入口判定上限且误差范围内入口稳定指向同一线路时才自动选线，否则按“本会话显式当前线 → 同物理站用户历史选择 → 一次线路选择”降级。陆家嘴等 16 个入口关系不完整的换乘站均被阻止误选；自动切线不会冒充用户显式线路，4 号线圈向、10 号线支线路径和手动方向保持合法。数据、运行时、定位、入口、首页交互、同步、个人中心和云开发 8 组回归、相关 JavaScript 语法及差异检查全部通过；微信开发者工具以真实模拟坐标显示嘉定北站约 3.8 公里、白银路约 3.9 公里、嘉定西站约 4.1 公里共 3 个候选，距离升序且全部不超过 5 公里，关闭后起点不变、顶部入口可重新打开。首页 WXML／WXSS 编译通过，控制台无错误或警告；VoiceOver 初始焦点与关闭后焦点恢复仍列入 iOS 真机验收。
- 2026-08-21：修复自选起点的“恢复定位”状态链路。开发者工具现场取证确认 `scope.userLocation: true`，当前模拟坐标 `31.37482, 121.26621` 因超过项目自定义的 1.2 公里站点匹配半径而返回“未匹配站点”，并非用户拒绝权限；旧界面在自选模式下遮住该失败状态，造成按钮无效的错觉。现有上次确认位置时，点击后立即恢复系统计算起点、保持当前浏览站不跳动并后台刷新；刷新未匹配时明确显示“未匹配站点｜重新定位”。没有缓存位置时仍等待定位成功再退出自选模式；权限已被拒绝时按微信官方规则打开小程序设置页引导重新授权。定位、运行时、数据、个人中心和首页交互回归、JavaScript 语法及差异检查全部通过；Nightly 模拟器实测从自选起点恢复后 `isManualAnchor=false`、计算起点回到淮海中路、当前浏览站保持不变，后台未匹配状态和重试入口可见，控制台无错误或警告。
- 2026-08-21：依据上海市道路运输管理局和市交通委公开材料，将 4 号线顶部方向从“内圈／外圈＋固定锚点下一站”精简为独立的“内圈／外圈”，避免拨轮移动时旧锚点文案造成误导并挤压左侧大站名；线路选择器的下拉字符改为 CSS 几何箭头，消除字体基线导致的下沉。依据微信官方更新机制，不新增手动“检查小程序更新”入口；厕所数据同步状态从关于页移到个人页并保持只读，关于页通过 `wx.getAccountInfoSync` 只在正式版展示真实版本号，开发版／体验版隐藏版本行。7 组本地回归、全量 JavaScript 语法、差异检查及首页／个人页／关于页 WXML、WXSS 编译均通过；Nightly 模拟器确认方向和箭头不再挤压站名、同步状态单行显示，关于页开发环境不再出现“开发版”，控制台无错误或警告。
- 2026-08-21：厕所导视标签最终收敛为地铁用户可直接理解的闸内／闸外／站内／站外、明确出口、原文明示的换乘通道和“方向＋车头／中部／车尾”；移除站厅／站厅层／候车厅／候车区／地面／连接通道及泛化“通道”，“站台”只在方向和出口均缺失时兜底。518 条记录中 41 条显示一个标签、477 条显示两个标签，不再出现三个标签；5 条保留“换乘通道”，10 条使用“站台”兜底。来源位置原文不变。数据、运行时、定位、首页交互、同步、个人中心和云开发 7 组回归、JavaScript 语法及差异检查全部通过。
- 2026-08-21：厕所卡片状态文案最终收敛为“卫生间”／“卫生间 · 多处”，移除 `WC`、箭头和“多个位置”的重复表达；同站区域聚合多条记录即显示“多处”，不因换乘线路不同而去重，点击整卡后继续按线路查看完整详情。暂无记录卡只显示“暂无厕所记录”；读屏、产品与设计文档、首页交互断言同步更新。数据、运行时、定位、首页交互、同步、个人中心和云开发 7 组回归、JavaScript 语法及差异检查全部通过；微信开发者工具 RC 2.02.2608031 的 iPhone 12／13 模拟器确认五张可见卡片、长站名、导视标签和 ETA 无挤压。本次未新增云函数、数据库或同步协议变更，控制台仍只有 RC 既有 `appServiceSDKScriptError timeout`。
- 2026-08-21：完成社会学／无障碍、UI／视觉和 UX／前端三路厕所卡片评估并落地。518 条 active 来源记录全部派生 1—2 个高置信度导视标签，覆盖闸内／闸外／站外、明确出口、换乘通道和“方向＋车头／中部／车尾”；21 条闸区缺失及来源冲突记录不再默认显示闸内，完整来源位置仍保留在抽屉。主卡移除长原文，改为固定单行标签与独立 ETA；同站区域聚合多条记录时使用“卫生间 · 多处”提示，不因换乘线路不同而去重，也不展示精确数量。有厕所卡在任意可见卡位均可整卡打开抽屉，换乘胶囊阻止冒泡，纵拖或有效横滑后短时抑制误触。数据、运行时、定位、首页交互、同步、个人中心和云开发 7 组回归、JavaScript 语法及差异检查全部通过；微信开发者工具 RC 2.02.2608031 的 iPhone 12／13 模拟器确认五站卡片标签与 ETA 无挤压，并从非焦点南京东路卡直接打开按 10／2 号线分组的完整位置抽屉。本次未新增认证、数据库、云函数、权限或同步协议变更；控制台仍只有 RC 既有 `appServiceSDKScriptError timeout`，未出现指向本次结构化卡片的错误。
- 2026-08-21：统一 5／10／11 号线顶部入口与站间切换位文案为“点击切换至 XXX 站支线”。文案由线路目录集中生成：航中路显示为“点击切换至航中路站支线”，虹桥火车站显示为“点击切换至虹桥火车站支线”，避免重复“站”；两个入口共用同一 `actionLabel`，旧的模板拼接和无消费者状态已清理。运行时与首页交互回归覆盖两类站名；模拟器临时将分支站指向当前可见的四川北路，确认顶部和站间长文案均完整显示，随后已恢复真实分支站龙溪路。
- 2026-08-20：完成关于页品牌与同步信息收敛。页面 Logo 替换为正式蓝绿地铁／厕所小程序图标，产品说明由“上海地铁厕所查询”改为不绑定首发城市的“地铁厕所查询”；同步状态改用与首页一致的绿色／蓝色圆点、当前年份紧凑时间和单行省略规则，并移除重复的手动数据检查入口，12 小时自动核对逻辑保持不变。同步、个人中心、云开发、数据、运行时、定位和首页交互 7 组本地验收、JavaScript 语法与差异检查全部通过；CloudBase 语义复核确认未修改认证、数据库、云函数、权限或同步协议。
- 2026-08-20：完成支线、环线和普通线路端点的整体交互调整。5／10／11 号线进入时直接选择默认主线路径，顶部以“当前端点／切换至另一端点”单入口替代“支线待确认”与双终点胶囊；分支站与下一站之间使用独立的半高支线切换位，原有一根站间竖轨在中点形成上下各半段，支线先向右下延伸 `45°` 再接水平段，“切换至另一端点”文案沿水平段排列并提供 `56rpx` 高快捷点击区，不展开另一套灰色站点卡片。切换位使用零高度中点锚点，不新增真实站点，也不改变列表高度、站点索引或轮盘吸附位置，从结构上移除了旧方案受 Skyline 轨道列裁剪及卡片层级遮挡的路径。4 号线继续按真实闭环和内／外圈建模，列表首尾增加更醒目的双箭头闭环符号，顶部辅以“环线 · 首尾相接”、底部辅以“继续循环”，闭环符号到相邻站点的轨道统一为一个正常站间距，并支持首末站互跳；非环线在可点击空心站点锚点外延伸一个正常站间距，并以同尺寸空心外环包裹同心实心点作为端点。10 号线运行时验证确认默认虹桥火车站路径、龙溪路切换后保持当前站、站序切换到航中路且提示反向指回虹桥火车站；4 号线闭环入口验证从宜山路端跳到上海体育馆端。7 组本地验收、JavaScript 语法、差异检查和微信开发者工具 WXML／WXSS 编译全部通过；模拟器临时将分支站指向当前可见的四川北路，确认 `45°` 斜线与水平段连续、目标文案完整显示在两张卡片之间，随后已恢复真实分支站龙溪路。真实分支站仍需用手机预览完成最终视觉复核。
- 2026-08-20：复核提交历史确认，个人页顶部栏移除与自定义 TabBar 启用曾被合并在同一提交中，但真机图标消失由后者引入，与“最近记录置顶”无关。现保留个人页精简布局和首页顶部卡片／数据刷新逻辑，删除 `custom-tab-bar`，移除两个 Tab 页的自定义实例钩子与额外底部占位，并恢复 `app.json` 原生 TabBar。回归断言锁定原生模式、两个入口的文字与 4 个图标路径，并禁止自定义目录或钩子重新出现；7 组本地验收、JavaScript 语法和差异检查全部通过。微信开发者工具 RC 2.02.2608031 重新编译后，首页与个人页截图确认两个原生图标及“首页／我的”文字均可见，个人页仍直接以“最近记录”开头，控制台未发现错误；仍需使用本次新预览包做最终真机复核。
- 2026-08-20：首页第三轮顶部控制调整完成。城市改为无下拉图标的胶囊并可打开仅含上海的城市面板；顶部压缩为两行，起点站名与“自选起点｜恢复定位”使用独立点击区；同步条改为绿色／蓝色圆点、当前年份紧凑时间和无胶囊的“刷新图标＋更新／更新中／重试”。`validate_data.js`、`validate_runtime.js`、`validate_location.js`、`validate_interaction.js`、`validate_sync.js`、`validate_profile.js`、`validate_cloud.js` 与 JavaScript 语法、差异检查全部通过。微信开发者工具 RC 2.02.2608031 普通编译后问题计数为 0，iPhone 12／13 模拟器确认首页正常渲染、同步说明不换行，城市和站名面板可打开；CloudBase 语义审查确认未修改认证、数据库、云函数或同步协议。
- 2026-08-20：按个人页真机截图移除与导航标题重复的顶部 logo／“我的”／副标题区块，并曾将原生 TabBar 改为项目内自定义实现。后续多轮真机预览均证实自定义导航图标不可见，因此该 TabBar 改造已回退；个人页精简布局继续保留。

- 2026-08-20：补齐 Skyline 轮盘的结束吸附与方向切换连续位置重映射。轮盘增加 `DRAGGING / DECELERATING / SNAPPING / RESETTING / REBASING` 状态、每次手势独立 session、`0.45—0.55` 候选滞回、固定 `snapTarget`、一次重试与 100ms 看门狗兜底；程序化 `scrollstart` 不再取消吸附，新手势可使旧完成事件失效。方向切换改用 `newPosition = newAnchorIndex + oldPosition - oldAnchorIndex`，在同一次 `setData` 中反转站点内容并更新 `scrollTop`，不再退回已吸附的离散索引。Nightly 运行时将轮盘分别停在 `+0.6` 与 `+0.4` 站距，前者稳定进入下一站、后者稳定回原站，最终站距误差均为 `0`、吸附目标正确清空；方向切换前后同一卡片偏移从 `-0.35` 保持为约 `-0.35`。WXML／WXSS 编译、Worklet 控制台检查和数据、运行时、定位、云开发、同步、个人中心、首页交互全量回归通过，仍待 iOS／Android 真机验证手指中途反向、临界慢拖和吸附中再次触摸。
- 2026-08-20：修复首页 Skyline 迁移后的弹层与横滑回归。线路选择、起点选择、定位候选和厕所详情列表补充明确视口高度及 `list-item` 声明，避免弹层外壳出现但内容滚动区高度为零；轮盘手势改为横向识别器与纵向 `scroll-view` 代理通过 `simultaneous-handlers` 协商，不再用横向组件错误代理纵向滚动。Nightly WXML／WXSS 编译通过，模拟器确认 19 条线路胶囊恢复显示、厕所抽屉滚动区高度恢复为约 434.4px，3 号线上海火车站左滑 72px 后实际切换到 4 号线；数据、运行时、定位、云开发、同步、个人中心和首页交互全量回归通过。
- 2026-08-20：首页纵向内核由 `swiper + WXS` 迁移为页面级 Skyline `scroll-view + Worklet`。轮盘只维护一个连续位置，固定第二卡位按距离对进入／离开卡片做余弦缩放和连续让位；原生滚动速度负责多站惯性，极端速度限制为每秒 22 个站位，结束后在 UI 线程吸附最近站。每次站点中心跨过焦点位只上报一次 detent，短音效改为预创建双实例并启用 WebAudio 底层，声音 48ms、触感 90ms 独立限频，停稳不再补响；横滑换乘迁入 Skyline 手势线程。微信 Nightly WXML／WXSS 编译、Skyline 模拟器渲染和 Worklet 控制台检查通过，当前索引 14“东宝兴路”实测位于第二卡位并放大，轮盘槽高按视口五等分为约 93.42px；数据、运行时、定位、云开发、同步、个人中心和首页交互全量回归通过。自动化元素触摸在 Skyline 上超时，连续手势、跨站声音节拍和速度参数仍列入 iOS／Android 真机验收。
- 2026-08-20：重做首页固定第二卡位 Dock 动效的坐标模型。根因是 WXS 同时使用手指 `clientY` 与 swiper `transition.dy` 两套位移估算，真机事件节奏不一致时会把焦点错误投射到下方多张卡片。现将两类事件都降为视图刷新信号，手势开始时锁定第二卡位的真实屏幕中心与实测相邻行距，每帧读取未参与缩放的 `station-row` 中心位置并按空间距离计算缩放；上滑半程只允许第二／第三张共享焦点，下滑半程只允许第一／第二张共享焦点，其余卡片缩放强度固定为 0。WXML／WXSS 编译、模拟器 WXS 运行时错误检查及数据、运行时、定位、首页交互、同步、个人中心回归均通过，等待真机手感复核。
- 2026-08-19：重新生成全部 TabBar 图标（home / home-active / usercenter / usercenter-active），统一使用苹果蓝 `#007AFF` 作为选中色、#8A8A8A / #A2A9B0 作为未选中色，并生成应用 logo（地铁隧道拱门 + 洗手间门 WC 符号）。「我的」页与关于页的 logo 由文字「针」替换为新图标图片。本地回归通过。
- 2026-08-19：「我的」页及关于子页主色由红色 `#D84A4A` 统一改为苹果蓝 `#007AFF`，覆盖品牌 logo、关于页 logo、音效／触感开关、清空、空态图标、最近记录展开入口与 OSM 许可链接；底部 TabBar 选中色同步改为苹果蓝，避免停在「我的」时仍显示红 tab。首页线路选择器选中文字与纠错页提交按钮／单选控件仍保留原红，作为后续一致性的待确认项。本地回归通过。
- 2026-08-19：个人中心最近记录升级为按物理站去重（基于站点坐标数据的 lineStationId → physicalStationId 映射，无法归并的记录回退线路站去重），同一站跨线路重复访问只保留最新线路上下文并置顶；列表默认折叠展示 5 条，超过时提供「查看全部 N 条」展开／收起入口，存储上限 20 条不变。新增 `validate_profile.js` 覆盖同站重复、跨线路去重、回退去重、折叠展开、点击跳转和清空，全部本地回归通过。
- 2026-08-19：重写首页第二卡位 Dock 动效链路：移除 `touchmove` 与 `swiper transition` 双写、`bindchange` 中途重建卡片和逻辑层逐帧 `setData`，新增 WXS 视图层连续焦点模块，仅用 `transform` 对焦点及相邻卡片做横纵缩放和让位，并在 `animationfinish` 后一次性提交业务索引。开发者工具运行时注入半卡位 transition，原卡／目标卡同时得到约 `scaleX(1.0325) scaleY(1.0275)`，临界点多次往返时 `currentIndex` 保持不变、第三张卡不参与放大；吸附结束后索引更新一次且 WXS 临时样式正确清空。WXML／WXSS 编译、模拟器 WXS 错误检查和数据、运行时、定位、首页交互、同步回归均通过。
- 2026-08-19：首页移除第二卡位的固定蓝绿背景光场，焦点仅保留实时尺寸插值与中性圆角阴影；建立“官方线路色用于轨道、圆点和胶囊识别，可读文字色用于 ETA 与线路文字”的双颜色规则，白底对比不足的线路色自动向中性深色校正到 4.5（3 号线 `#FFD100` 校正为 `#897616`）；右上线路选择器改为双列彩色胶囊。iPhone 12／13 模拟器确认页面背景干净、3 号线文字清晰且线路选择器不再全黑，WXML／WXSS 编译、模拟器 console 和数据、运行时、定位、首页交互、同步回归均通过。
- 2026-08-19：修复固定光场生效但相邻卡片在手指拖动时不缩放的问题。运行时取证确认旧实现只依赖 `swiper transition`，真实 `touchstart → touchmove` 后两张卡 `motionStyle` 仍为空；现将纵向 `touchmove` 直接接入同一距离插值，`transition` 仅补充惯性阶段，并把焦点最大缩放从 `1.025` 提升为 `1.055`。微信开发者工具在拖动半程实测当前卡／目标卡分别为 `scale(1.0271)`／`scale(1.0279)`，宽度、高度、间距和柔光同步变化；WXML／WXSS 编译、模拟器 console 和全部本地回归均通过。
- 2026-08-19：修正站点轮盘实时放大中心漂移：第二卡位增加固定蓝绿圆角光场，卡片按其与固定卡位的距离使用余弦曲线缩放，壳层宽度、圆角、柔光以及内部高度和留白同步插值，形成接近 macOS Dock 的进入放大／离开缩小效果。iPhone 12／13 模拟器重新编译并完成纵向滚动吸附，焦点保持在第二卡位、同屏 5 站布局正常；交互回归覆盖中心最大、一个站距恢复、半程双卡对称及动画结束复位，全部本地回归通过。
- 2026-08-19：首页站点轮盘改为滑动过程实时插值：基于 `swiper` 的位移进度，只连续更新当前卡与目标卡的宽度、缩放、让位间距和圆角柔光，吸附结束后清理临时样式；首页 ETA 跟随当前线路色，多厕所抽屉 ETA 跟随厕所所属线路色。iPhone 12／13 模拟器确认 13 号线 ETA 正确显示为线路橙色，页面编译和布局正常；数据、运行时、定位、首页交互与同步回归均通过，其中交互回归覆盖半程拖动和动画结束复位。
- 2026-08-19：根据真机截图修复焦点背光矩形裁切和首页底部大块空白：移除会超出 `swiper-item` 的外扩蓝绿阴影，改为卡片壳层内部 `4rpx` 圆角柔光与内阴影；移除原生 TabBar 页面重复的 `112rpx + safe-area` 底部占位，只保留 `16rpx` 间距。iPhone 12／13 模拟器确认直角光块和底部大块空白均已消失，第二位焦点与同屏 5 站保持不变；数据、运行时、定位、首页交互和同步回归均通过。
- 2026-08-19：首页真机验收第一轮视觉调整完成：站点轮盘改为第二位固定焦点，按轮盘实高动态保留一个前置站位；常规卡片与焦点卡片使用两档宽度，焦点上下卡片额外让位，所有卡片阴影统一继承圆角轮廓，焦点背光改为低饱和淡蓝绿。iPhone 12／13 模拟器确认焦点位于第二位且同屏仍为 5 站，本地数据、运行时、定位、首页交互和同步回归均通过；具体间距与背光强度等待真机视觉复核。调试器唯一错误仍为 RC 既有 `appServiceSDKScriptError timeout`，未出现指向本次新增结构或样式的错误。
- 2026-08-19：删除未注册的 CloudBase 快速开始示例页面、示例组件及无引用图片／图标，保留 4 个业务 Tab 图标和 2.4 KB 提示音；`miniprogram/` 由约 1.9 MB 降至约 640 KB。微信开发者工具重新扫描显示“小程序表现良好，未发现代码质量问题”，主包大小和单资源不超过 200 KB 两项均已通过，普通编译后调试器错误为 0。
- 2026-08-18：首页第二轮交互调整完成：iPhone 12／13 模拟器同屏固定显示 5 个站点，恢复苹果式大圆角、柔和阴影和原字号；左侧轨道保持连续且不参与焦点缩放，仅右侧当前卡片轻微放大。
- 2026-08-18：智能定位站恢复定位针，其他站点圆点可快捷锁定手动计算起点；拨轮吸附仍只更新派生目标。方向切换保持计算起点与当前物理站，只反转展示序列，并按切换后 `targetIndex < originIndex` 判定掉头，回转图标仅在当前高亮卡显示。
- 2026-08-18：微信开发者工具实测镇坪路横滑可在 3 号线与 4 号线之间往返切换，圆点锁定手动锚点后可恢复智能定位；纵向拖动、短横滑和取消手势回归通过。
- 2026-08-18：换乘站增加当前线及全部可换乘线路胶囊，点击与横滑保持同一物理站和原计算起点；多厕所抽屉按厕所自身线路分组、当前线置顶，并以对应官方线路色显示小面积标识。
- 2026-08-18：首页主列表移除红色纠错入口；抽屉改为“信息有误？反馈”弱链接，并显式携带厕所自己的线路与线路站点 ID。新增交互验收脚本，连同数据、运行时、定位、云开发和同步回归全部通过。
- 2026-08-18：微信开发者工具普通编译并完成首页渲染；RC 2.02.2608031 仍偶发既有 `appServiceSDKScriptError timeout`，已保留为真机／稳定版复核项，本次未出现 WXML、WXSS 或页面逻辑编译错误。
- 2026-08-18：完成按路径线路同步的本地实现：固定 12 小时检查、跨线批量、城市级原子缓存、single-flight、手动最多 3 次尝试、5 分钟成功冷却、10 秒失败防连点和 15 分钟自动退避。
- 2026-08-18：首页与关于页已接入绿色／橙色／灰色同步状态及手动刷新；厕所维护、关闭、状态未知覆盖按生效和过期时间应用，常驻前台每分钟仅本地刷新展示、不发网络请求。
- 2026-08-18：新版 `metroRestroomApi` 已部署并进入 `Active / Available`；`syncRestroomStatus` 真实调用通过全量 19 条变化、全量 19 条未变化、2／8 号线混合变化和未知线路拒绝，旧 `getDataVersion` 回归通过。
- 2026-08-18：`data_versions` 已按“不可变线路快照先写并回读、manifest 最后发布”写入上海首批 19 条空覆盖快照；manifest 含 19 条 SHA-256 版本映射且固定 `ttlSeconds: 43200`，兼容文档 `restroom-data` 保留。
- 2026-08-18：全量本地回归通过：`validate_data.js`、`validate_runtime.js`、`validate_location.js`、`validate_cloud.js`、`validate_sync.js`、相关 JavaScript 语法检查及首页／关于页生命周期冒烟；部署后权限回读确认 `data_versions` 仍为 `ADMINONLY`、函数规则仍为 `invoke: true`。
- 2026-08-18：已将按线路同步、12 小时自动检查、5 分钟手动刷新冷却、本地 ETA、固定时间格式和同步状态文案写入 PRD、云开发说明及研究结论文档，作为本轮实现基线。
- 2026-08-18：环境 `metro-restroom-d4goyb1fq3f9df0b3` 已关联 AppID `wx5b829b09e2d525b3`；创建 `data_versions`、`correction_reports`、`correction_rate_limits`，三者权限均回读为 `ADMINONLY`。
- 2026-08-18：认证配置回读确认用户名密码、邮箱和手机号登录均关闭，匿名身份与 `Metro洗手间` 小程序 OpenID provider 已启用；项目无需实际用户名或密码。
- 2026-08-18：`metroRestroomApi` 已部署为 Node.js 18.15 事件函数，调用规则回读为 `invoke: true`；函数内部仍以 `OPENID` 是否存在作为纠错提交门槛，数据库仅保存不可逆哈希。
- 2026-08-18：微信开发者工具真实提交 3 号线东宝兴路纠错成功；云函数 433ms 返回 `success: true`，数据库新增 1 条 `pending` 测试记录，未保存用户名、密码或原始 `OPENID`。
- 2026-08-18：修复 `wx-server-sdk 3.0.1` 默认对不存在文档抛错导致首次提交失败的问题；本地云开发验收脚本再次通过。
- 2026-08-18：CloudBase Code Review 完成；实际应用链路未发现鉴权、集合权限、SDK 或敏感运行时数据回显问题，未启用快速开始模板已列入上线前清理项。
- 2026-08-18：云开发本地验收通过，覆盖 19 条线路、518 个纠错上下文、匿名哈希、幂等重试、事务限流、未知动作拒绝、网络错误降级和本地草稿。
- 2026-08-18：微信开发者工具普通编译为 0 错误、0 警告；首页“纠错”可自动带入 3 号线东宝兴路，选择问题类型并填写 13 字说明后提交按钮由禁用变为可用。
- 2026-08-18：OSM 坐标构建连续两次输出 SHA-256 `6ee9d5cfabe27f440dafd17874f2c78406eab1422cbdeb2521f590ce797c5992`；覆盖 411／411 个物理站和 518／518 条 active 线路站，相邻站距离为 570—9,216 米。
- 2026-08-18：微信开发者工具重新普通编译通过，首页正常显示“开启智能定位”入口，调试器为 0 错误、0 警告；关于页正确展示厕所 519 条、坐标 411 站及 ODbL 署名，许可链接复制操作通过。
- 2026-08-18：新增定位逻辑验收通过，覆盖距离计算、换乘歧义、站外、授权、拒绝、超时和设置恢复；确认 518 条 active 线路站合并为 411 个物理站。

- 2026-08-17：Excel 导入连续执行两次输出 SHA-256 一致；生成数据与导入报告均可确定性复现。
- 2026-08-17：数据验收脚本通过，确认 519 条源记录、519 个唯一 `lineStationId` 和 131 条去重后的同名换乘候选。
- 2026-08-17：运行时验收脚本通过，确认 19 条线路、518 个可浏览站点和 518 条 active 源厕所记录全部进入页面数据链路。
- 2026-08-17：微信开发者工具实测当时旧版首页、19 线选择器、5 号线“支线待确认／终点选择”和个人中心，编译面板 Errors 为 0；该支线交互已于 2026-08-20 被默认主线与单入口切换方案取代。
